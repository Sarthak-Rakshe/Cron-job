{
  "name": "render-cron-pinger",
  "version": "1.0.0",
  "private": true,
  "description": "Cron job to ping backend with 120s timeout every 13 minutes on Render",
  "type": "commonjs",
  "scripts": {
    "start": "node scripts/ping.js"
  },
  "engines": {
    "node": ">=18"
  }
}
